CREATE TABLE `carolina_jewerly_db`.`suscripcion` (
  `codigo_suscripcion` INT NOT NULL,
  `codigo_usuario` VARCHAR(10) NOT NULL,
  `estado` VARCHAR(20) NOT NULL,
  PRIMARY KEY (`codigo_suscripcion`));
